# Paper2Code 项目操作说明

## 1. 项目简介

Paper2Code 是一个旨在将科研论文自动转换为结构化代码库的项目。它通过与大型语言模型 (LLM) 交互，分阶段完成从论文解析、规划设计到代码生成的过程。

项目主要包含三个核心阶段：
1.  **规划 (Planning)**: 基于输入的论文内容，生成总体计划、架构设计、模块与任务设计，并输出一个 `config.yaml` 文件。
2.  **分析 (Analyzing)**: 针对规划阶段确定的每个待实现文件，结合论文和规划文档，生成更详细的逻辑分析。
3.  **编码 (Coding)**: 基于详细逻辑分析和已有代码上下文，为每个文件生成实际代码。

## 2. 环境准备

### 2.1 Python 环境
确保您已安装 Python (推荐版本 3.8 或更高)。

### 2.2 安装依赖
(如果项目包含 `requirements.txt` 文件，请按如下方式安装依赖)
```bash
pip install -r requirements.txt
```
*注意：目前 `requirements.txt` 可能尚未提供，请根据实际错误信息安装所需库，例如 `openai`, `python-dotenv`, `PyYAML` 等。*

### 2.3 环境变量配置
项目支持通过 `.env` 文件或直接设置环境变量来配置一些常用参数。创建一个名为 `.env` 的文件在项目根目录，并按以下格式添加所需变量：

```env
OPENAI_API_KEY="sk-your_openai_api_key"
# OPENAI_API_BASE="https://api.openai.com/v1" # 如果使用OpenAI官方API，此行可不填或注释掉
OPENAI_API_BASE="https://openrouter.ai/api/v1" # 示例：使用OpenRouter
LLM_MODEL_NAME="google/gemini-2.5-flash-preview" # 示例模型
LLM_TEMPERATURE="0.7"

# 可选：如果不想每次都通过命令行指定，可以在此设置
# PAPER_NAME="MyPaper"
# PAPER_JSON_PATH="./examples/mypaper/mypaper_pdf.json"
# BASE_OUTPUT_DIR="./outputs"
# LATEX_ZIP_PATH="./examples/mypaper/mypaper_latex.zip"
```

**重要环境变量:**
*   `OPENAI_API_KEY`: 必需。您的 OpenAI API 密钥。
*   `OPENAI_API_BASE`: 可选。API 的基础 URL，用于兼容第三方服务或本地部署的 LLM。若命令行、环境变量均未指定，则默认为 `https://openrouter.ai/api/v1` (如 `main.py` 中所定义)。
*   `LLM_MODEL_NAME`: 可选。使用的 LLM 模型名称。若命令行、环境变量均未指定，则默认为 `google/gemini-2.5-flash-preview` (如 `main.py` 中所定义)。
*   `LLM_TEMPERATURE`: 可选。LLM 的 temperature 参数。若命令行、环境变量均未指定，则默认为 `0.7` (如 `main.py` 中所定义)。
*   `PAPER_NAME`: 可选。论文的名称，将用于创建输出子目录和命名文件。如果未在命令行中提供，则会尝试读取此环境变量。
*   `PAPER_JSON_PATH`: 可选。从 PDF 转换得到的原始 JSON 文件的路径。如果未在命令行中提供，则会尝试读取此环境变量。
*   `BASE_OUTPUT_DIR`: 可选。所有输出文件的基础目录。默认为 `./outputs`。
*   `LATEX_ZIP_PATH`: 可选。论文的 LaTeX 源文件压缩包路径。

参数的加载优先级为：**命令行参数 > 环境变量 (`.env` 文件加载的) > `main.py` 中定义的 argparse 默认值/应用级默认值**。

## 3. 运行项目

通过执行项目根目录下的 `main.py` 脚本来启动整个流程。

### 3.1 命令行参数

```bash
python main.py [OPTIONS]
```

**必需参数:**
*   `--paper_name TEXT`: 论文的名称 (例如：Medagents)。如果未通过命令行或 `PAPER_NAME` 环境变量提供，程序将报错。
*   `--paper_json_path TEXT`: 从 PDF 转换得到的原始 JSON 文件的路径。如果未通过命令行或 `PAPER_JSON_PATH` 环境变量提供，程序将报错。

**可选参数 (参见 `main.py` 以获取最新默认值):**
*   `--base_output_dir TEXT`: 所有输出文件的基础目录。 (argparse 默认: `./outputs`)
*   `--latex_zip_path TEXT`: (可选) 论文的 LaTeX 源文件压缩包路径。 (argparse 无默认值)
*   `--openai_api_key TEXT`: (可选) OpenAI API 密钥。如果未提供，将尝试从环境变量 `OPENAI_API_KEY` 读取。 (argparse 无默认值)
*   `--openai_api_base TEXT`: (可选) OpenAI API 的基础 URL。 (argparse 无默认值, 应用级默认: `https://openrouter.ai/api/v1`)
*   `--llm_model_name TEXT`: (可选) 使用的 LLM 模型名称。 (argparse 无默认值, 应用级默认: `google/gemini-2.5-flash-preview`)
*   `--llm_temperature FLOAT`: (可选) LLM 的 temperature 参数。 (argparse 无默认值, 应用级默认: `0.7`)

### 3.2 示例运行命令

假设您的论文信息如下：
*   论文名称: `AlphaFold`
*   JSON 路径: `./data/alphafold/alphafold.json`
*   LaTeX 压缩包路径: `./data/alphafold/alphafold_latex.zip`
*   API Key 已在 `.env` 文件中设置。

您可以这样运行：
```bash
python main.py \
    --paper_name AlphaFold \
    --paper_json_path ./data/alphafold/alphafold.json \
    --latex_zip_path ./data/alphafold/alphafold_latex.zip \
    --base_output_dir ./results
```
如果 `PAPER_NAME` 和 `PAPER_JSON_PATH` 已在 `.env` 中设置，可以简化为：
```bash
python main.py --latex_zip_path ./data/alphafold/alphafold_latex.zip --base_output_dir ./results
```
如果所有必需和可选参数均通过 `.env` 或默认值得到满足，可以直接运行：
```bash
python main.py
```
(但至少 `paper_name` 和 `paper_json_path` 需要通过命令行或环境变量提供其一)。

## 4. 项目结构

### 4.1 主要代码目录 (`src/`)
*   `pipeline.py`: 包含 `Pipeline` 类，负责编排整个处理流程，调用各个阶段的 Agent。
*   `planning_agent.py`: 包含 `PlanningAgent` 类，执行规划阶段。
*   `analyzing_agent.py`: 包含 `AnalyzingAgent` 类，执行分析阶段。
*   `coding_agent.py`: 包含 `CodingAgent` 类，执行编码阶段。
*   `llm_handler.py`: 包含 `LLMClient` 类，封装与 LLM API 的交互。
*   `paper_parser.py`: 包含 `JSONCleaner` 类等，用于处理和解析输入的论文文件。
*   `utils.py`: 包含项目共享的辅助函数 (例如 `ensure_dir_exists`)。

### 4.2 输出目录结构 (`outputs/` 或指定的 `--base_output_dir`)
项目运行后，会在指定的输出目录下创建一个以 `[PAPER_NAME]_output` 命名的子目录，例如 `outputs/AlphaFold_output/`。该目录下包含：
*   `planning_artifacts/`: 存放规划阶段生成的 Markdown 和 YAML 文件。
    *   `[PAPER_NAME]_planning_overall_plan.md`
    *   `[PAPER_NAME]_planning_architecture_design.md`
    *   `[PAPER_NAME]_planning_logic_and_task_design.md`
    *   `[PAPER_NAME]_planning_config.yaml` (也会复制到 `[PAPER_NAME]_output` 根目录)
*   `analyzing_artifacts/`: 存放分析阶段为每个文件生成的详细逻辑分析文本。
    *   `[FILENAME]_simple_analysis.txt`
*   `coding_artifacts/`: 存放编码阶段 LLM 的原始输出。
    *   `[FILENAME]_coding.txt`
*   `output_repo_dir/`: 存放最终生成的代码库。
    *   `[FILENAME]` (例如 `main.py`, `utils.py` 等)
*   根目录下的中间产物文件 (位于 `[PAPER_NAME]_output`):
    *   `[PAPER_NAME]_planning_config.yaml`: 规划阶段生成的配置文件。
    *   `[PAPER_NAME]_planning_trajectories.json`: 规划阶段完整的 LLM 对话历史。
    *   `[FILENAME]_simple_analysis_response.json`: 分析阶段单个文件分析的原始 LLM 响应。
    *   `[FILENAME]_simple_analysis_trajectories.json`: 分析阶段单个文件分析的对话历史。

## 5. 主要流程详解

### 5.1 启动与配置加载
1.  `main.py` 解析命令行参数和环境变量。参数优先级为：命令行 > 环境变量 > argparse/应用内默认值。
2.  实例化 `Pipeline` 类，传入所有配置。
3.  `Pipeline` 初始化时会创建基础输出目录 `[base_output_dir]/[paper_name]_output/` 以及其中的 `planning_artifacts`, `analyzing_artifacts`, `coding_artifacts`, `output_repo_dir` 子目录。

### 5.2 论文解析 (可选预处理)
*   项目当前主要依赖用户提供的 `paper_json_path` (必需) 和可选的 `latex_zip_path`。
*   `JSONCleaner` (在 `src/paper_parser.py` 中) 封装了清理从 PDF 转换的 JSON 数据的逻辑，但其调用点在当前的 `Pipeline` 主流程中尚未明确，可能作为前置步骤或按需调用。
*   实际的论文内容 (JSON 结构或从 LaTeX 提取的文本) 会被加载并传递给后续阶段的 Agent。

### 5.3 规划阶段 (`PlanningAgent`)
1.  **输入**: `paper_name`, 论文内容 (从 `paper_json_path` 和可选的 `latex_zip_path` 加载)，`llm_client`, 输出路径等。
2.  **处理**:
    *   通过 `LLMClient` 与 LLM 进行多轮交互 (默认四轮)，分别生成：
        1.  总体计划 (`[PAPER_NAME]_planning_overall_plan.md`)
        2.  架构设计 (`[PAPER_NAME]_planning_architecture_design.md`)
        3.  逻辑与任务设计 (`[PAPER_NAME]_planning_logic_and_task_design.md`)
        4.  `config.yaml` 内容 (`[PAPER_NAME]_planning_config.yaml`)
    *   保存上述产物到 `planning_artifacts/` 子目录，并将 `config.yaml` 也复制到 `[PAPER_NAME]_output` 根目录。
    *   保存完整的 LLM 对话历史到 `[PAPER_NAME]_output/[PAPER_NAME]_planning_trajectories.json`。
3.  **输出**: `[PAPER_NAME]_planning_config.yaml` 文件路径，各项规划文档的路径，LLM 对话历史路径。

### 5.4 分析阶段 (`AnalyzingAgent`)
1.  **输入**:
    *   `planning_config.yaml` 的路径 (来自规划阶段)。
    *   `[PAPER_NAME]_planning_trajectories.json` 的路径 (用于提取规划概览、架构设计、任务设计字符串，并从中解析出待办文件列表和逻辑分析描述映射)。
    *   论文内容，`llm_client`，输出路径等。
2.  **处理**:
    *   加载 `config.yaml` 和规划轨迹。
    *   从规划产物中解析出待分析的文件列表及其初步描述 (目前通过简化的辅助方法实现)。
    *   对于每个待分析文件：
        *   准备 LLM 提示，包含论文、各项规划文档、`config.yaml` 和针对当前文件的指令。
        *   调用 LLM 生成详细的逻辑分析。
        *   将分析文本保存到 `analyzing_artifacts/[FILENAME]_simple_analysis.txt`。
        *   将原始 LLM 响应保存到 `[PAPER_NAME]_output/[FILENAME]_simple_analysis_response.json`。
        *   将对话历史保存到 `[PAPER_NAME]_output/[FILENAME]_simple_analysis_trajectories.json`。
3.  **输出**: 分析产物的保存路径。

### 5.5 编码阶段 (`CodingAgent`)
1.  **输入**:
    *   `planning_config.yaml` 的路径。
    *   `[PAPER_NAME]_planning_trajectories.json` 的路径 (用于再次提取规划细节和待编码文件列表)。
    *   分析阶段产物：通过加载每个待编码文件对应的 `[FILENAME]_simple_analysis_response.json` 来获取详细逻辑分析。
    *   论文内容，`llm_client`，最终代码输出目录 `output_repo_dir` 的路径等。
    *   **已生成的其他文件的代码** (通过 `self.done_file_code_map` 动态累积，作为上下文)。
2.  **处理**:
    *   加载 `config.yaml` 和规划轨迹，解析待编码文件列表。
    *   为每个文件加载其详细逻辑分析。
    *   遍历待编码文件列表 (通常会跳过 `config.yaml` 本身)。
    *   对于每个文件：
        *   准备 LLM 提示，包含论文、规划文档、`config.yaml`、当前文件的详细逻辑分析，以及已生成的其他代码。
        *   调用 LLM 生成该文件的代码。
        *   从 LLM 响应中提取纯代码块。
        *   将原始 LLM 输出保存到 `coding_artifacts/[FILENAME]_coding.txt`。
        *   将提取的代码保存到最终的代码仓库目录 `output_repo_dir/[FILENAME]`。
        *   将生成的代码加入 `self.done_file_code_map` 供后续文件编码时参考。
3.  **输出**: 生成的代码库所在的 `output_repo_dir` 路径。

## 6. `config.yaml` 文件
此文件在规划阶段由 LLM 生成，通常命名为 `[PAPER_NAME]_planning_config.yaml`。它包含了项目的关键配置信息，例如待生成的文件列表、文件间的依赖关系（可能）、以及其他可能的项目元数据。它在分析和编码阶段都会被加载和使用，是串联各阶段任务的核心。

## 7. 注意事项
*   确保您的 OpenAI API 密钥有效且有足够配额。API Base URL 和模型名称可以根据需要进行配置。
*   根据论文的复杂性和大小，LLM 调用可能需要较长时间并产生相应费用。
*   输出目录会包含大量中间文件和最终代码，请确保有足够的磁盘空间。
*   LLM 生成的代码质量依赖于提示的质量、模型的选择以及论文本身的清晰度。生成后可能需要人工审查和修改。
*   项目中用于从规划轨迹中提取特定信息 (如规划概览、架构设计、任务设计) 的辅助函数目前可能是占位或简化的，未来可能会移至 `src/utils.py` 并进一步完善。

---
这份操作说明是根据截至目前对项目代码和结构的理解编写的。随着项目的迭代，具体细节可能会发生变化。请参考最新的代码和 README (如果提供) 获取最准确的信息。 